import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

export const StyleLogoBox = styled(Box)`
	display: flex;
	align-items: center;
	min-width: 170px;
	margin-right: 15px;

	a {
		text-decoration: none;
	}
`;
