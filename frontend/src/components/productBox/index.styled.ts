import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";
import { theme } from "../../app.styled";

export const SProductBox = styled(Box)`
	width: 100%;
	height: 100%;
	background-color: ${theme.palette.background.default};
`;
