import { Box, Container, Grid, styled } from "@mui/material";

export const SHomeContainer = styled(Container)`
	max-width: 1280px;
`;

export const StyledGridItem = styled(Grid)``;

export const StyledProductBox = styled(Box)`
	border: solid 1px black;
	height: 350px;
`;
